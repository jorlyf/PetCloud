namespace api.Infrastructure.Utils
{
	public static class AppDirectories
	{
		public readonly static string Root = $"{Environment.CurrentDirectory}";
		public readonly static string CloudData = $"{Root}\\CloudData";
	}
}
